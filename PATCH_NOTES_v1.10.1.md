## SmolLyfe v1.10.1

- cleaned up character creation spacing and moved name randomization into a smaller inline utility control under the name fields
- simplified year-advanced notifications into a lighter dismissible banner and unified the notification shell
- standardized the deep-route identity strip so name and cash use one shared header treatment
- renamed the main route back to `Occupation` and removed the temporary `Life path` section from the home feed
- removed visible activity-cap text while keeping the internal per-activity impact cap
- added shared accordion and route identity layout components to reduce one-off nested UI patterns
- standardized stress and popularity presentation around cleaner percentage-based progress rows
- reduced nested box clutter in Occupation and tightened classmates/social card hierarchy
- softened `Study Harder` stress penalties by making the larger stress hit happen only once per school stage
- added gentle passive bond growth over time for classmates, teammates, and coworkers
- added a baseline reusable NPC prompt modal flow with payload-backed outcomes for awkward social/identity questions
